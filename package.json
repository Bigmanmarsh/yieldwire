{
  "name": "yieldwire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "YieldWire — the autonomous agent that watches Base stablecoin yield and messages you when the board changes. No custody. No trades. Receipts on everything.",
  "scripts": {
    "agent": "node src/run.js"
  }
}
